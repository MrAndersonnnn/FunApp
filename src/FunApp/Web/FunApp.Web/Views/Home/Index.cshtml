@model FunApp.Web.Models.Home.IndexViewModel

@{
    ViewData["Title"] = "Home Page";
}

@foreach (var joke in Model.Jokes)
{
    <div class="panel panel-default">
        <div class="panel-body">
            @joke.Content
        </div>
        <div class="panel-footer">@joke.CategoryName</div>
    </div>
}